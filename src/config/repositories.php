<?php

return array(

    /*
    |--------------------------------------------------------------------------
    | Simple honeypot
    |--------------------------------------------------------------------------
    |
    | Used to catch potential spammers
    |
    */

    'enable_honeypot' => true,

    /*
    |--------------------------------------------------------------------------
    | Reserved names
    |--------------------------------------------------------------------------
    |
    | An array of protected names. This is used to prevent users from
    | picking an administrator's name. To disable this feature set to
    | an empty array.
    |
    */

    'reserved_names' => array('administrator'),
);
